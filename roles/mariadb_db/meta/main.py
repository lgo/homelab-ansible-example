# NB(joey): This is here so every container that makes a database will also
# ensure mariadb is actually running. I excluded this for brevity.
#
# dependencies:
#   - role: containers/databases/mariadb
